#include<cstdio>
#include<iostream>
#include<vector>
#include<string>
using namespace std;
int main() {
    int n;
    string temp;
    scanf("%d",&n);
    vector<string> str;
    str.clear();
    while (n--)
    {
        cin >> temp;
        vector.insert(temp);
    }
    
    
}